# Custom runtime image with Docker CLI for repos that require Docker.
FROM docker.openhands.dev/openhands/runtime:1.4-nikolaik

RUN apt-get update && \
    apt-get install -y --no-install-recommends docker-ce-cli docker-compose-plugin && \
    rm -rf /var/lib/apt/lists/*
